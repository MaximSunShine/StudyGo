package sqlstore

//не актуально

/*
type ConfigStore struct {
	DatabaseUrl string `toml:"database_url"` //добавить новые параметры, данную удалить
}

func NewConfig() *ConfigStore {
	return &ConfigStore{
		DatabaseUrl: "host=localhost port=5430 user=postgres password=domcrat dbname=restapi_dev sslmode=disable",
	}
}
*/
